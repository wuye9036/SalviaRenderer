#include <gtest/gtest.h>

#include <salvia/core/async_object.h>

using namespace salvia::core;

TEST(salvia_core, create_async_object) {
  // async_stats stats{pipeline_statistics};
  // int cnt = 2;
  // accumulate<ID>(stats, cnt)
}