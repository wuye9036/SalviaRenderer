SALVIA_CHECK_BUILD_WITH_UNICODE()

INCLUDE_DIRECTORIES(
	${EFLIB_HOME_PATH}
)

set ( SOURCE_FILES logrout.cpp )
ADD_EXECUTABLE( logrout ${SOURCE_FILES} )
TARGET_LINK_LIBRARIES( logrout EFLIB )

SALVIA_CONFIG_OUTPUT_PATHS(logrout)