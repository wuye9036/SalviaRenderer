################################################
# !!!! DO NOT DELETE ANY FIELD OF THIS FILE !!!!
################################################

# ----------------------------------------------------------------
# Notes of parameters:
#   boost_root:
#      The root directory of boost.
#
#	   Only support boost 1.70 and later.
#
#   build_root:
#      The root directory for build and intermediate files.
#
#   install_root:
#      Root directory to installing all output of binaries.
#      <install_root>/bin is for DLL and executable files,
#      <install_root>/lib is directory for all static libraries.
#
#   arch:
#      Target architecture.
#      Available values are:
#            x86
#            x64
#
#   toolset:
#      Toolset for compiling.
#      Available values are:
#            msvc-<major>.<minor>     for e.g.: 'msvc-14.2'
#            gcc
#
#	   Supported compilers:
#			 MSVC 14.2 or later. (VS 2019.2)
#			 MinGW (32bit) 9.0 and later	(with 'gcc' toolset)
#	
#	toolset_dir:
#		for MSVC:
#			toolset_dir is the folder includes vcvars64.bat
#	    for gcc and clang:
#		    if compiler is not located in environment variable "PATH",
#		    please specify toolset_dir for your compiler which includes the gcc/g++.exe.
#		    Additional, if you installed MinGW in C:\MinGW\, script could find it without this variable.
#
#   config:
#      Build configuration.
#      It could be one of 'Debug', 'Release', 'MinSizeRel', 'RelWithDebInfo'.
#   
#   cmake:
#      Path which include CMAKE executable file.
# --------------------------------------------------------------

build_root		= '.\\build'
install_root	= './'
arch			= 'x64'
toolset			= 'msvc-14.2'
toolset_dir		= r'D:\Software\VS2019\VC\Auxiliary\Build'
config			= 'RelWithDebInfo'
cmake			= r'D:\Software\CMake\bin\cmake.exe'	
